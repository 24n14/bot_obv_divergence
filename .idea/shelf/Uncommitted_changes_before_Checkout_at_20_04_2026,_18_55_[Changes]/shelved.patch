Index: find_obv_divergence.py
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>import numpy as np\nimport talib\nimport logging\n\nlogger = logging.getLogger(__name__)\n\n\ndef find_obv_divergence(high, low, close, volume, lookback=20):\n    \"\"\"\n    Ищет дивергенцию между ценой и OBV с улучшенной логикой детекции.\n    Теперь проверяет ВСЕ пары экстремумов, а не только последнюю.\n    \"\"\"\n\n    # 1. Рассчитываем OBV и его сглаженную версию\n    obv = talib.OBV(close, volume)\n    obv_sma = talib.SMA(obv, timeperiod=5)\n\n    # 2. Берём срезы для анализа\n    price = close[-lookback:]\n    obv_slice = obv[-lookback:]\n    obv_smooth = obv_sma[-lookback:]\n\n    # 3. Находим локальные экстремумы\n    price_min_idx, price_max_idx = find_local_extremes(price)\n    obv_min_idx, obv_max_idx = find_local_extremes(obv_smooth)\n\n    # 4. Ищем дивергенции - проверяем ВСЕ пары, выбираем сильнейшую\n    best_signal = None\n    best_confidence = 0.0\n    best_details = {}\n\n    # === БЫЧЬЯ дивергенция ===\n    if len(price_min_idx) >= 2 and len(obv_min_idx) >= 2:\n        # Проверяем все пары минимумов, не только последнюю\n        for i in range(len(price_min_idx) - 1):\n            prev_min = price_min_idx[i]\n            curr_min = price_min_idx[i + 1]\n\n            # Находим соответствующие OBV минимумы в интервале\n            obv_mins_in_range = [idx for idx in obv_min_idx if prev_min <= idx <= curr_min]\n\n            if len(obv_mins_in_range) >= 2:\n                # Берём первый и последний минимум OBV в этом диапазоне\n                prev_obv_min = obv_mins_in_range[0]\n                curr_obv_min = obv_mins_in_range[-1]\n\n                # Проверяем условие дивергенции: цена вниз, OBV вверх\n                price_down = price[curr_min] < price[prev_min]\n                obv_up = obv_smooth[curr_obv_min] > obv_smooth[prev_obv_min]\n\n                if price_down and obv_up:\n                    confidence, details = calculate_confidence_dynamic(\n                        price=price,\n                        obv=obv_smooth,\n                        price_idx=(prev_min, curr_min),\n                        obv_idx=(prev_obv_min, curr_obv_min),\n                        high=high[-lookback:],\n                        low=low[-lookback:],\n                        close=price,\n                        volume=volume[-lookback:],\n                        divergence_type='bullish'\n                    )\n\n                    # Выбираем сигнал с наибольшей уверенностью\n                    if confidence > best_confidence:\n                        best_confidence = confidence\n                        best_signal = 'bullish'\n                        best_details = details\n                        logger.debug(\n                            f\"\uD83D\uDFE2 BULLISH дивергенция найдена: \"\n                            f\"price {price[prev_min]:.2f} → {price[curr_min]:.2f}, \"\n                            f\"OBV {obv_smooth[prev_obv_min]:.0f} → {obv_smooth[curr_obv_min]:.0f}, \"\n                            f\"confidence: {confidence:.2%}\"\n                        )\n\n    # === МЕДВЕЖЬЯ дивергенция ===\n    if len(price_max_idx) >= 2 and len(obv_max_idx) >= 2:\n        # Проверяем все пары максимумов\n        for i in range(len(price_max_idx) - 1):\n            prev_max = price_max_idx[i]\n            curr_max = price_max_idx[i + 1]\n\n            # Находим соответствующие OBV максимумы в интервале\n            obv_maxs_in_range = [idx for idx in obv_max_idx if prev_max <= idx <= curr_max]\n\n            if len(obv_maxs_in_range) >= 2:\n                prev_obv_max = obv_maxs_in_range[0]\n                curr_obv_max = obv_maxs_in_range[-1]\n\n                # Проверяем условие дивергенции: цена вверх, OBV вниз\n                price_up = price[curr_max] > price[prev_max]\n                obv_down = obv_smooth[curr_obv_max] < obv_smooth[prev_obv_max]\n\n                if price_up and obv_down:\n                    confidence, details = calculate_confidence_dynamic(\n                        price=price,\n                        obv=obv_smooth,\n                        price_idx=(prev_max, curr_max),\n                        obv_idx=(prev_obv_max, curr_obv_max),\n                        high=high[-lookback:],\n                        low=low[-lookback:],\n                        close=price,\n                        volume=volume[-lookback:],\n                        divergence_type='bearish'\n                    )\n\n                    # Выбираем сигнал с наибольшей уверенностью\n                    if confidence > best_confidence:\n                        best_confidence = confidence\n                        best_signal = 'bearish'\n                        best_details = details\n                        logger.debug(\n                            f\"\uD83D\uDD34 BEARISH дивергенция найдена: \"\n                            f\"price {price[prev_max]:.2f} → {price[curr_max]:.2f}, \"\n                            f\"OBV {obv_smooth[prev_obv_max]:.0f} → {obv_smooth[curr_obv_max]:.0f}, \"\n                            f\"confidence: {confidence:.2%}\"\n                        )\n\n    if best_signal:\n        return best_signal, best_confidence, best_details\n    return None, 0.0, {}\n\n\ndef find_local_extremes(arr):\n    \"\"\"Находит индексы локальных минимумов и максимумов\"\"\"\n    min_idx = []\n    max_idx = []\n    for i in range(2, len(arr) - 2):\n        if arr[i] < arr[i - 1] and arr[i] < arr[i - 2] and \\\n                arr[i] < arr[i + 1] and arr[i] < arr[i + 2]:\n            min_idx.append(i)\n        if arr[i] > arr[i - 1] and arr[i] > arr[i - 2] and \\\n                arr[i] > arr[i + 1] and arr[i] > arr[i + 2]:\n            max_idx.append(i)\n    return min_idx, max_idx\n\n\ndef calculate_confidence_dynamic(price, obv, price_idx, obv_idx,\n                                 high, low, close, volume, divergence_type):\n    \"\"\"\n    Рассчитывает уверенность в дивергенции на основе 6 рыночных факторов.\n    УЛУЧШЕНО: Повышены коэффициенты для более реалистичных значений confidence.\n\n    Максимальная уверенность: 1.0\n    Факторы:\n    - Сила цены: 0-0.25 (увеличено с 0.2)\n    - Сила OBV: 0-0.25 (увеличено с 0.2)\n    - Временное совпадение: 0-0.15\n    - Объём: 0-0.15\n    - Волатильность: 0-0.1 (уменьшено)\n    - Тренд: 0-0.1 (уменьшено)\n    \"\"\"\n    confidence = 0.0\n    details = {}\n\n    prev_price_idx, curr_price_idx = price_idx\n    prev_obv_idx, curr_obv_idx = obv_idx\n\n    # === ФАКТОР 1: Сила расхождения цены (0 - 0.25) ===\n    if divergence_type == 'bullish':\n        price_change = (price[curr_price_idx] - price[prev_price_idx]) / price[prev_price_idx]\n        # Чем сильнее цена упала (отрицательное значение), тем лучше для бычьей дивергенции\n        strength = min(abs(price_change) / 0.03, 1.0)  # 3% падения дают максимум (было 5%)\n    else:  # bearish\n        price_change = (price[curr_price_idx] - price[prev_price_idx]) / price[prev_price_idx]\n        strength = min(price_change / 0.03, 1.0)  # 3% роста даю�� максимум (было 5%)\n\n    factor1 = strength * 0.25  # Увеличено с 0.2\n    confidence += factor1\n    details['price_strength'] = round(factor1, 3)\n\n    # === ФАКТОР 2: Сила расхождения OBV (0 - 0.25) ===\n    if divergence_type == 'bullish':\n        # OBV должен быть выше предыдущего минимума\n        obv_change = (obv[curr_obv_idx] - obv[prev_obv_idx]) / abs(obv[prev_obv_idx] + 1e-6)\n        strength = min(obv_change / 0.02, 1.0)  # 2% роста OBV дают максимум (было 3%)\n    else:\n        obv_change = (obv[prev_obv_idx] - obv[curr_obv_idx]) / abs(obv[prev_obv_idx] + 1e-6)\n        strength = min(obv_change / 0.02, 1.0)  # 2% падения OBV дают максимум (было 3%)\n\n    factor2 = strength * 0.25  # Увеличено с 0.2\n    confidence += factor2\n    details['obv_strength'] = round(factor2, 3)\n\n    # === ФАКТОР 3: Временное совпадение экстремумов (0 - 0.15) ===\n    time_diff = abs(curr_price_idx - curr_obv_idx)\n    if time_diff == 0:\n        factor3 = 0.15\n    elif time_diff <= 2:\n        factor3 = 0.12\n    elif time_diff <= 5:\n        factor3 = 0.08\n    else:\n        factor3 = 0.0\n    confidence += factor3\n    details['time_alignment'] = factor3\n\n    # === ФАКТОР 4: Объём на экстремуме (0 - 0.15) ===\n    # Берём объём за 3 свечи вокруг экстремума\n    volume_window = volume[max(0, curr_price_idx - 1):min(len(volume), curr_price_idx + 2)]\n    avg_volume = np.mean(volume_window)\n    historical_avg = np.mean(volume[max(0, curr_price_idx - 20):curr_price_idx])\n\n    if historical_avg > 0:\n        volume_ratio = avg_volume / historical_avg\n        if divergence_type == 'bullish':\n            # При бычьей дивергенции на минимуме должен быть высокий объём (паника)\n            if volume_ratio > 1.5:\n                factor4 = 0.15\n            elif volume_ratio > 1.2:\n                factor4 = 0.12\n            elif volume_ratio > 0.9:\n                factor4 = 0.08\n            else:\n                factor4 = 0.05\n        else:\n            # При медвежьей дивергенции на максимуме высокий объём подтверждает разворот\n            if volume_ratio > 1.5:\n                factor4 = 0.15\n            elif volume_ratio > 1.2:\n                factor4 = 0.12\n            elif volume_ratio > 0.9:\n                factor4 = 0.08\n            else:\n                factor4 = 0.05\n    else:\n        factor4 = 0.05\n    confidence += factor4\n    details['volume_confirm'] = factor4\n\n    # === ФАКТОР 5: Волатильность рынка (0 - 0.1) ===\n    # ATR (Average True Range) как % от цены\n    try:\n        atr = talib.ATR(high, low, close, timeperiod=14)[-1]\n        current_price = close[-1]\n        if current_price > 0:\n            volatility_pct = (atr / current_price) * 100\n        else:\n            volatility_pct = 0\n    except:\n        volatility_pct = 2.0  # Значение по умолчанию\n\n    if 1.0 < volatility_pct < 5.0:  # Оптимальная волатильность (была 1.5-4.0)\n        factor5 = 0.1\n    elif 0.5 < volatility_pct <= 1.0 or 5.0 <= volatility_pct < 10.0:  # Нормальная\n        factor5 = 0.06\n    elif volatility_pct <= 0.5 or volatility_pct >= 10.0:  # Слишком тихо или шумно\n        factor5 = 0.02\n    else:\n        factor5 = 0.04\n    confidence += factor5\n    details['volatility'] = factor5\n\n    # === ФАКТОР 6: Тренд более высокого таймфрейма (0 - 0.1) ===\n    # Проверяем положение цены относительно EMA50\n    try:\n        if len(close) >= 50:\n            ema50 = talib.EMA(close, timeperiod=50)[-1]\n        else:\n            ema50 = np.mean(close)  # Если недостаточно данных, используем простое среднее\n    except:\n        ema50 = close[-1]\n\n    current_price = close[-1]\n\n    if divergence_type == 'bullish':\n        # Бычья дивергенция сильнее, если цена ниже EMA50 (отскок от перепроданности)\n        if current_price < ema50 * 0.95:  # на 5% ниже EMA\n            factor6 = 0.10\n        elif current_price < ema50:\n            factor6 = 0.07\n        else:\n            factor6 = 0.03\n    else:\n        # Медвежья дивергенция сильнее, если цена выше EMA50\n        if current_price > ema50 * 1.05:  # на 5% выше EMA\n            factor6 = 0.10\n        elif current_price > ema50:\n            factor6 = 0.07\n        else:\n            factor6 = 0.03\n\n    confidence += factor6\n    details['trend_alignment'] = factor6\n\n    # Итоговый confidence ограничиваем 1.0\n    confidence = min(confidence, 1.0)\n    details['total'] = round(confidence, 3)\n\n    return confidence, details\n\n\n# ============ ПРИМЕР ИСПОЛЬЗОВАНИЯ ============\n'''\n# Допустим, df — ваш DataFrame со свечами\nclose_prices = df['close'].values\nhigh_prices = df['high'].values\nlow_prices = df['low'].values\nvolumes = df['volume'].values\n\nsignal, confidence, details = find_obv_divergence(\n    high_prices, low_prices, close_prices, volumes, lookback=50\n)\n\nif signal:\n    print(f\"Сигнал: {signal}\")\n    print(f\"Уверенность: {confidence:.2%}\")\n    print(\"Детали расчёта:\")\n    for key, value in details.items():\n        print(f\"  {key}: {value}\")\n\n    if confidence > 0.65:\n        print(\"✅ ВХОДИМ в сделку\")\n    elif confidence > 0.5:\n        print(\"⚠\uFE0F Входим с половинным объёмом\")\n    else:\n        print(\"❌ Пропускаем — сигнал слабый\")\n'''
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/find_obv_divergence.py b/find_obv_divergence.py
--- a/find_obv_divergence.py	
+++ b/find_obv_divergence.py	
@@ -5,133 +5,154 @@
 logger = logging.getLogger(__name__)
 
 
-def find_obv_divergence(high, low, close, volume, lookback=20):
+def find_obv_divergence(high, low, close, volume, lookback=50):
     """
     Ищет дивергенцию между ценой и OBV с улучшенной логикой детекции.
-    Теперь проверяет ВСЕ пары экстремумов, а не только последнюю.
+    КРИТИЧЕСКИ УЛУЧШЕНО: Проверяет несколько окон lookback для поиска дивергенций разного масштаба.
     """
 
-    # 1. Рассчитываем OBV и его сглаженную версию
+    # Используем весь доступный диапазон, не ограничиваясь lookback
+    total_candles = len(close)
+
+    # Рассчитываем OBV и его сглаженную версию на ПОЛНЫХ данных
     obv = talib.OBV(close, volume)
     obv_sma = talib.SMA(obv, timeperiod=5)
 
-    # 2. Берём срезы для анализа
-    price = close[-lookback:]
-    obv_slice = obv[-lookback:]
-    obv_smooth = obv_sma[-lookback:]
-
-    # 3. Находим локальные экстремумы
-    price_min_idx, price_max_idx = find_local_extremes(price)
-    obv_min_idx, obv_max_idx = find_local_extremes(obv_smooth)
-
-    # 4. Ищем дивергенции - проверяем ВСЕ пары, выбираем сильнейшую
     best_signal = None
     best_confidence = 0.0
     best_details = {}
 
-    # === БЫЧЬЯ дивергенция ===
-    if len(price_min_idx) >= 2 and len(obv_min_idx) >= 2:
-        # Проверяем все пары минимумов, не только последнюю
-        for i in range(len(price_min_idx) - 1):
-            prev_min = price_min_idx[i]
-            curr_min = price_min_idx[i + 1]
+    # Проверяем несколько временных окон для поиска дивергенций разных масштабов
+    # 1. Агрессивный: последние 30 свечей (150 мин на 5m)
+    # 2. Нормальный: последние 50 свечей (250 мин на 5m)
+    # 3. Долгосрочный: последние 100 свечей (500 мин на 5m)
+
+    lookback_windows = [30, 50, 100]
+
+    for window_lookback in lookback_windows:
+        if total_candles < window_lookback:
+            continue
+
+        # Берём срезы для анализа
+        price = close[-window_lookback:]
+        obv_slice = obv[-window_lookback:]
+        obv_smooth = obv_sma[-window_lookback:]
+
+        # Находим локальные экстремумы
+        price_min_idx, price_max_idx = find_local_extremes(price)
+        obv_min_idx, obv_max_idx = find_local_extremes(obv_smooth)
+
+        logger.debug(f"🔬 Окно {window_lookback}: price_mins={len(price_min_idx)}, "
+                     f"price_maxs={len(price_max_idx)}, "
+                     f"obv_mins={len(obv_min_idx)}, obv_maxs={len(obv_max_idx)}")
+
+        # === БЫЧЬЯ дивергенция ===
+        if len(price_min_idx) >= 2 and len(obv_min_idx) >= 2:
+            # Проверяем все пары минимумов, не только последнюю
+            for i in range(len(price_min_idx) - 1):
+                prev_min = price_min_idx[i]
+                curr_min = price_min_idx[i + 1]
 
-            # Находим соответствующие OBV минимумы в интервале
-            obv_mins_in_range = [idx for idx in obv_min_idx if prev_min <= idx <= curr_min]
+                # Находим соответствующие OBV минимумы в интервале
+                obv_mins_in_range = [idx for idx in obv_min_idx if prev_min <= idx <= curr_min]
 
-            if len(obv_mins_in_range) >= 2:
-                # Берём первый и последний минимум OBV в этом диапазоне
-                prev_obv_min = obv_mins_in_range[0]
-                curr_obv_min = obv_mins_in_range[-1]
+                if len(obv_mins_in_range) >= 2:
+                    prev_obv_min = obv_mins_in_range[0]
+                    curr_obv_min = obv_mins_in_range[-1]
 
-                # Проверяем условие дивергенции: цена вниз, OBV вверх
-                price_down = price[curr_min] < price[prev_min]
-                obv_up = obv_smooth[curr_obv_min] > obv_smooth[prev_obv_min]
+                    # Проверяем условие дивергенции: цена вниз, OBV вверх
+                    price_down = price[curr_min] < price[prev_min]
+                    obv_up = obv_smooth[curr_obv_min] > obv_smooth[prev_obv_min]
 
-                if price_down and obv_up:
-                    confidence, details = calculate_confidence_dynamic(
-                        price=price,
-                        obv=obv_smooth,
-                        price_idx=(prev_min, curr_min),
-                        obv_idx=(prev_obv_min, curr_obv_min),
-                        high=high[-lookback:],
-                        low=low[-lookback:],
-                        close=price,
-                        volume=volume[-lookback:],
-                        divergence_type='bullish'
-                    )
+                    if price_down and obv_up:
+                        confidence, details = calculate_confidence_dynamic(
+                            price=price,
+                            obv=obv_smooth,
+                            price_idx=(prev_min, curr_min),
+                            obv_idx=(prev_obv_min, curr_obv_min),
+                            high=high[-window_lookback:],
+                            low=low[-window_lookback:],
+                            close=price,
+                            volume=volume[-window_lookback:],
+                            divergence_type='bullish'
+                        )
 
-                    # Выбираем сигнал с наибольшей уверенностью
-                    if confidence > best_confidence:
-                        best_confidence = confidence
-                        best_signal = 'bullish'
-                        best_details = details
-                        logger.debug(
-                            f"🟢 BULLISH дивергенция найдена: "
-                            f"price {price[prev_min]:.2f} → {price[curr_min]:.2f}, "
-                            f"OBV {obv_smooth[prev_obv_min]:.0f} → {obv_smooth[curr_obv_min]:.0f}, "
-                            f"confidence: {confidence:.2%}"
-                        )
+                        logger.debug(f"🟢 BULLISH в окне {window_lookback}: "
+                                     f"price {price[prev_min]:.2f}→{price[curr_min]:.2f}, "
+                                     f"OBV {obv_smooth[prev_obv_min]:.0f}→{obv_smooth[curr_obv_min]:.0f}, "
+                                     f"confidence={confidence:.2%}")
+
+                        if confidence > best_confidence:
+                            best_confidence = confidence
+                            best_signal = 'bullish'
+                            best_details = details
 
-    # === МЕДВЕЖЬЯ дивергенция ===
-    if len(price_max_idx) >= 2 and len(obv_max_idx) >= 2:
-        # Проверяем все пары максимумов
-        for i in range(len(price_max_idx) - 1):
-            prev_max = price_max_idx[i]
-            curr_max = price_max_idx[i + 1]
+        # === МЕДВЕЖЬЯ дивергенция ===
+        if len(price_max_idx) >= 2 and len(obv_max_idx) >= 2:
+            for i in range(len(price_max_idx) - 1):
+                prev_max = price_max_idx[i]
+                curr_max = price_max_idx[i + 1]
 
-            # Находим соответствующие OBV максимумы в интервале
-            obv_maxs_in_range = [idx for idx in obv_max_idx if prev_max <= idx <= curr_max]
+                obv_maxs_in_range = [idx for idx in obv_max_idx if prev_max <= idx <= curr_max]
 
-            if len(obv_maxs_in_range) >= 2:
-                prev_obv_max = obv_maxs_in_range[0]
-                curr_obv_max = obv_maxs_in_range[-1]
+                if len(obv_maxs_in_range) >= 2:
+                    prev_obv_max = obv_maxs_in_range[0]
+                    curr_obv_max = obv_maxs_in_range[-1]
 
-                # Проверяем условие дивергенции: цена вверх, OBV вниз
-                price_up = price[curr_max] > price[prev_max]
-                obv_down = obv_smooth[curr_obv_max] < obv_smooth[prev_obv_max]
+                    price_up = price[curr_max] > price[prev_max]
+                    obv_down = obv_smooth[curr_obv_max] < obv_smooth[prev_obv_max]
 
-                if price_up and obv_down:
-                    confidence, details = calculate_confidence_dynamic(
-                        price=price,
-                        obv=obv_smooth,
-                        price_idx=(prev_max, curr_max),
-                        obv_idx=(prev_obv_max, curr_obv_max),
-                        high=high[-lookback:],
-                        low=low[-lookback:],
-                        close=price,
-                        volume=volume[-lookback:],
-                        divergence_type='bearish'
-                    )
+                    if price_up and obv_down:
+                        confidence, details = calculate_confidence_dynamic(
+                            price=price,
+                            obv=obv_smooth,
+                            price_idx=(prev_max, curr_max),
+                            obv_idx=(prev_obv_max, curr_obv_max),
+                            high=high[-window_lookback:],
+                            low=low[-window_lookback:],
+                            close=price,
+                            volume=volume[-window_lookback:],
+                            divergence_type='bearish'
+                        )
 
-                    # Выбираем сигнал с наибольшей уверенностью
-                    if confidence > best_confidence:
-                        best_confidence = confidence
-                        best_signal = 'bearish'
-                        best_details = details
-                        logger.debug(
-                            f"🔴 BEARISH дивергенция найдена: "
-                            f"price {price[prev_max]:.2f} → {price[curr_max]:.2f}, "
-                            f"OBV {obv_smooth[prev_obv_max]:.0f} → {obv_smooth[curr_obv_max]:.0f}, "
-                            f"confidence: {confidence:.2%}"
-                        )
+                        logger.debug(f"🔴 BEARISH в окне {window_lookback}: "
+                                     f"price {price[prev_max]:.2f}→{price[curr_max]:.2f}, "
+                                     f"OBV {obv_smooth[prev_obv_max]:.0f}→{obv_smooth[curr_obv_max]:.0f}, "
+                                     f"confidence={confidence:.2%}")
+
+                        if confidence > best_confidence:
+                            best_confidence = confidence
+                            best_signal = 'bearish'
+                            best_details = details
 
     if best_signal:
+        logger.info(f"✅ Выбран сигнал: {best_signal.upper()} с confidence {best_confidence:.2%}")
         return best_signal, best_confidence, best_details
     return None, 0.0, {}
 
 
 def find_local_extremes(arr):
-    """Находит индексы локальных минимумов и максимумов"""
+    """
+    Находит индексы локальных минимумов и максимумов.
+    УЛУЧШЕНО: Более мягкие критерии для поиска на разных масштабах.
+    """
     min_idx = []
     max_idx = []
+
+    if len(arr) < 5:
+        return min_idx, max_idx
+
     for i in range(2, len(arr) - 2):
+        # Проверяем минимум: значение ниже соседей
         if arr[i] < arr[i - 1] and arr[i] < arr[i - 2] and \
                 arr[i] < arr[i + 1] and arr[i] < arr[i + 2]:
             min_idx.append(i)
+
+        # Проверяем максимум: значение выше соседей
         if arr[i] > arr[i - 1] and arr[i] > arr[i - 2] and \
                 arr[i] > arr[i + 1] and arr[i] > arr[i + 2]:
             max_idx.append(i)
+
     return min_idx, max_idx
 
 
@@ -139,16 +160,7 @@
                                  high, low, close, volume, divergence_type):
     """
     Рассчитывает уверенность в дивергенции на основе 6 рыночных факторов.
-    УЛУЧШЕНО: Повышены коэффициенты для более реалистичных значений confidence.
-
-    Максимальная уверенность: 1.0
-    Факторы:
-    - Сила цены: 0-0.25 (увеличено с 0.2)
-    - Сила OBV: 0-0.25 (увеличено с 0.2)
-    - Временное совпадение: 0-0.15
-    - Объём: 0-0.15
-    - Волатильность: 0-0.1 (уменьшено)
-    - Тренд: 0-0.1 (уменьшено)
+    УЛУЧШЕНО: Более строгая оценка силы дивергенции.
     """
     confidence = 0.0
     details = {}
@@ -156,29 +168,27 @@
     prev_price_idx, curr_price_idx = price_idx
     prev_obv_idx, curr_obv_idx = obv_idx
 
-    # === ФАКТОР 1: Сила расхождения цены (0 - 0.25) ===
+    # === ФАКТОР 1: Сила расхождения цены (0 - 0.3) ===
     if divergence_type == 'bullish':
         price_change = (price[curr_price_idx] - price[prev_price_idx]) / price[prev_price_idx]
-        # Чем сильнее цена упала (отрицательное значение), тем лучше для бычьей дивергенции
-        strength = min(abs(price_change) / 0.03, 1.0)  # 3% падения дают максимум (было 5%)
+        strength = min(abs(price_change) / 0.02, 1.0)  # 2% падения = максимум
     else:  # bearish
         price_change = (price[curr_price_idx] - price[prev_price_idx]) / price[prev_price_idx]
-        strength = min(price_change / 0.03, 1.0)  # 3% роста даю�� максимум (было 5%)
+        strength = min(price_change / 0.02, 1.0)
 
-    factor1 = strength * 0.25  # Увеличено с 0.2
+    factor1 = strength * 0.3  # Основной фактор
     confidence += factor1
     details['price_strength'] = round(factor1, 3)
 
-    # === ФАКТОР 2: Сила расхождения OBV (0 - 0.25) ===
+    # === ФАКТОР 2: Сила расхождения OBV (0 - 0.3) ===
     if divergence_type == 'bullish':
-        # OBV должен быть выше предыдущего минимума
         obv_change = (obv[curr_obv_idx] - obv[prev_obv_idx]) / abs(obv[prev_obv_idx] + 1e-6)
-        strength = min(obv_change / 0.02, 1.0)  # 2% роста OBV дают максимум (было 3%)
+        strength = min(obv_change / 0.015, 1.0)  # 1.5% роста OBV = максимум
     else:
         obv_change = (obv[prev_obv_idx] - obv[curr_obv_idx]) / abs(obv[prev_obv_idx] + 1e-6)
-        strength = min(obv_change / 0.02, 1.0)  # 2% падения OBV дают максимум (было 3%)
+        strength = min(obv_change / 0.015, 1.0)
 
-    factor2 = strength * 0.25  # Увеличено с 0.2
+    factor2 = strength * 0.3
     confidence += factor2
     details['obv_strength'] = round(factor2, 3)
 
@@ -195,41 +205,25 @@
     confidence += factor3
     details['time_alignment'] = factor3
 
-    # === ФАКТОР 4: Объём на экстремуме (0 - 0.15) ===
-    # Берём объём за 3 свечи вокруг экстремума
+    # === ФАКТОР 4: Объём на экстремуме (0 - 0.1) ===
     volume_window = volume[max(0, curr_price_idx - 1):min(len(volume), curr_price_idx + 2)]
     avg_volume = np.mean(volume_window)
     historical_avg = np.mean(volume[max(0, curr_price_idx - 20):curr_price_idx])
 
     if historical_avg > 0:
         volume_ratio = avg_volume / historical_avg
-        if divergence_type == 'bullish':
-            # При бычьей дивергенции на минимуме должен быть высокий объём (паника)
-            if volume_ratio > 1.5:
-                factor4 = 0.15
-            elif volume_ratio > 1.2:
-                factor4 = 0.12
-            elif volume_ratio > 0.9:
-                factor4 = 0.08
-            else:
-                factor4 = 0.05
+        if volume_ratio > 1.3:
+            factor4 = 0.1
+        elif volume_ratio > 1.0:
+            factor4 = 0.06
         else:
-            # При медвежьей дивергенции на максимуме высокий объём подтверждает разворот
-            if volume_ratio > 1.5:
-                factor4 = 0.15
-            elif volume_ratio > 1.2:
-                factor4 = 0.12
-            elif volume_ratio > 0.9:
-                factor4 = 0.08
-            else:
-                factor4 = 0.05
+            factor4 = 0.03
     else:
-        factor4 = 0.05
+        factor4 = 0.03
     confidence += factor4
     details['volume_confirm'] = factor4
 
-    # === ФАКТОР 5: Волатильность рынка (0 - 0.1) ===
-    # ATR (Average True Range) как % от цены
+    # === ФАКТОР 5: Волатильность рынка (0 - 0.08) ===
     try:
         atr = talib.ATR(high, low, close, timeperiod=14)[-1]
         current_price = close[-1]
@@ -238,81 +232,47 @@
         else:
             volatility_pct = 0
     except:
-        volatility_pct = 2.0  # Значение по умолчанию
+        volatility_pct = 2.0
 
-    if 1.0 < volatility_pct < 5.0:  # Оптимальная волатильность (была 1.5-4.0)
-        factor5 = 0.1
-    elif 0.5 < volatility_pct <= 1.0 or 5.0 <= volatility_pct < 10.0:  # Нормальная
-        factor5 = 0.06
-    elif volatility_pct <= 0.5 or volatility_pct >= 10.0:  # Слишком тихо или шумно
-        factor5 = 0.02
-    else:
+    if 0.8 < volatility_pct < 6.0:
+        factor5 = 0.08
+    elif 0.3 < volatility_pct <= 0.8 or 6.0 <= volatility_pct < 12.0:
         factor5 = 0.04
+    else:
+        factor5 = 0.01
     confidence += factor5
     details['volatility'] = factor5
 
-    # === ФАКТОР 6: Тренд более высокого таймфрейма (0 - 0.1) ===
-    # Проверяем положение цены относительно EMA50
+    # === ФАКТОР 6: Тренд более высокого таймфрейма (0 - 0.08) ===
     try:
         if len(close) >= 50:
             ema50 = talib.EMA(close, timeperiod=50)[-1]
         else:
-            ema50 = np.mean(close)  # Если недостаточно данных, используем простое среднее
+            ema50 = np.mean(close)
     except:
         ema50 = close[-1]
 
     current_price = close[-1]
 
     if divergence_type == 'bullish':
-        # Бычья дивергенция сильнее, если цена ниже EMA50 (отскок от перепроданности)
-        if current_price < ema50 * 0.95:  # на 5% ниже EMA
-            factor6 = 0.10
+        if current_price < ema50 * 0.95:
+            factor6 = 0.08
         elif current_price < ema50:
-            factor6 = 0.07
+            factor6 = 0.05
         else:
-            factor6 = 0.03
+            factor6 = 0.02
     else:
-        # Медвежья дивергенция сильнее, если цена выше EMA50
-        if current_price > ema50 * 1.05:  # на 5% выше EMA
-            factor6 = 0.10
+        if current_price > ema50 * 1.05:
+            factor6 = 0.08
         elif current_price > ema50:
-            factor6 = 0.07
+            factor6 = 0.05
         else:
-            factor6 = 0.03
+            factor6 = 0.02
 
     confidence += factor6
     details['trend_alignment'] = factor6
 
-    # Итоговый confidence ограничиваем 1.0
     confidence = min(confidence, 1.0)
     details['total'] = round(confidence, 3)
 
-    return confidence, details
-
-
-# ============ ПРИМЕР ИСПОЛЬЗОВАНИЯ ============
-'''
-# Допустим, df — ваш DataFrame со свечами
-close_prices = df['close'].values
-high_prices = df['high'].values
-low_prices = df['low'].values
-volumes = df['volume'].values
-
-signal, confidence, details = find_obv_divergence(
-    high_prices, low_prices, close_prices, volumes, lookback=50
-)
-
-if signal:
-    print(f"Сигнал: {signal}")
-    print(f"Уверенность: {confidence:.2%}")
-    print("Детали расчёта:")
-    for key, value in details.items():
-        print(f"  {key}: {value}")
-
-    if confidence > 0.65:
-        print("✅ ВХОДИМ в сделку")
-    elif confidence > 0.5:
-        print("⚠️ Входим с половинным объёмом")
-    else:
-        print("❌ Пропускаем — сигнал слабый")
-'''
\ No newline at end of file
+    return confidence, details
\ No newline at end of file
Index: .idea/modules.xml
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/.idea/modules.xml b/.idea/modules.xml
new file mode 100644
--- /dev/null	
+++ b/.idea/modules.xml	
@@ -0,0 +1,8 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<project version="4">
+  <component name="ProjectModuleManager">
+    <modules>
+      <module fileurl="file://$PROJECT_DIR$/.idea/bot_obv_divergence.iml" filepath="$PROJECT_DIR$/.idea/bot_obv_divergence.iml" />
+    </modules>
+  </component>
+</project>
\ No newline at end of file
Index: .idea/bot_obv_divergence.iml
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/.idea/bot_obv_divergence.iml b/.idea/bot_obv_divergence.iml
new file mode 100644
--- /dev/null	
+++ b/.idea/bot_obv_divergence.iml	
@@ -0,0 +1,10 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<module type="PYTHON_MODULE" version="4">
+  <component name="NewModuleRootManager">
+    <content url="file://$MODULE_DIR$">
+      <excludeFolder url="file://$MODULE_DIR$/.venv" />
+    </content>
+    <orderEntry type="inheritedJdk" />
+    <orderEntry type="sourceFolder" forTests="false" />
+  </component>
+</module>
\ No newline at end of file
Index: .idea/inspectionProfiles/Project_Default.xml
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/.idea/inspectionProfiles/Project_Default.xml b/.idea/inspectionProfiles/Project_Default.xml
new file mode 100644
--- /dev/null	
+++ b/.idea/inspectionProfiles/Project_Default.xml	
@@ -0,0 +1,14 @@
+<component name="InspectionProjectProfileManager">
+  <profile version="1.0">
+    <option name="myName" value="Project Default" />
+    <inspection_tool class="PyPackageRequirementsInspection" enabled="true" level="WARNING" enabled_by_default="true">
+      <option name="ignoredPackages">
+        <value>
+          <list size="1">
+            <item index="0" class="java.lang.String" itemvalue="sequential" />
+          </list>
+        </value>
+      </option>
+    </inspection_tool>
+  </profile>
+</component>
\ No newline at end of file
Index: .idea/vcs.xml
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/.idea/vcs.xml b/.idea/vcs.xml
new file mode 100644
--- /dev/null	
+++ b/.idea/vcs.xml	
@@ -0,0 +1,6 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<project version="4">
+  <component name="VcsDirectoryMappings">
+    <mapping directory="$PROJECT_DIR$" vcs="Git" />
+  </component>
+</project>
\ No newline at end of file
Index: .idea/misc.xml
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/.idea/misc.xml b/.idea/misc.xml
new file mode 100644
--- /dev/null	
+++ b/.idea/misc.xml	
@@ -0,0 +1,7 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<project version="4">
+  <component name="Black">
+    <option name="sdkName" value="Python 3.12 (bot_obv_divergence)" />
+  </component>
+  <component name="ProjectRootManager" version="2" project-jdk-name="Python 3.12 (bot_obv_divergence)" project-jdk-type="Python SDK" />
+</project>
\ No newline at end of file
Index: .idea/workspace.xml
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/.idea/workspace.xml b/.idea/workspace.xml
new file mode 100644
--- /dev/null	
+++ b/.idea/workspace.xml	
@@ -0,0 +1,166 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<project version="4">
+  <component name="AutoImportSettings">
+    <option name="autoReloadType" value="SELECTIVE" />
+  </component>
+  <component name="ChangeListManager">
+    <list default="true" id="70af1cd2-67dc-4623-85ec-ad4cb930180c" name="Changes" comment="mod loger in trading.py">
+      <change afterPath="$PROJECT_DIR$/.idea/bot_obv_divergence.iml" afterDir="false" />
+      <change afterPath="$PROJECT_DIR$/.idea/inspectionProfiles/Project_Default.xml" afterDir="false" />
+      <change afterPath="$PROJECT_DIR$/.idea/inspectionProfiles/profiles_settings.xml" afterDir="false" />
+      <change afterPath="$PROJECT_DIR$/.idea/misc.xml" afterDir="false" />
+      <change afterPath="$PROJECT_DIR$/.idea/modules.xml" afterDir="false" />
+      <change afterPath="$PROJECT_DIR$/.idea/vcs.xml" afterDir="false" />
+      <change afterPath="$PROJECT_DIR$/.idea/workspace.xml" afterDir="false" />
+    </list>
+    <option name="SHOW_DIALOG" value="false" />
+    <option name="HIGHLIGHT_CONFLICTS" value="true" />
+    <option name="HIGHLIGHT_NON_ACTIVE_CHANGELIST" value="false" />
+    <option name="LAST_RESOLUTION" value="IGNORE" />
+  </component>
+  <component name="FileTemplateManagerImpl">
+    <option name="RECENT_TEMPLATES">
+      <list>
+        <option value="Python Script" />
+      </list>
+    </option>
+  </component>
+  <component name="Git.Settings">
+    <option name="RECENT_BRANCH_BY_REPOSITORY">
+      <map>
+        <entry key="$PROJECT_DIR$" value="master" />
+      </map>
+    </option>
+    <option name="RECENT_GIT_ROOT_PATH" value="$PROJECT_DIR$" />
+  </component>
+  <component name="ProjectColorInfo">{
+  &quot;associatedIndex&quot;: 5
+}</component>
+  <component name="ProjectId" id="3CW0ziI2bU3lkEb5L1oO94kYEvH" />
+  <component name="ProjectLevelVcsManager" settingsEditedManually="true">
+    <ConfirmationsSetting value="2" id="Add" />
+  </component>
+  <component name="ProjectViewState">
+    <option name="hideEmptyMiddlePackages" value="true" />
+    <option name="showLibraryContents" value="true" />
+  </component>
+  <component name="PropertiesComponent"><![CDATA[{
+  "keyToString": {
+    "Python.start.executor": "Debug",
+    "RunOnceActivity.ShowReadmeOnStart": "true",
+    "git-widget-placeholder": "features"
+  }
+}]]></component>
+  <component name="RunManager">
+    <configuration name="start" type="PythonConfigurationType" factoryName="Python" temporary="true" nameIsGenerated="true">
+      <module name="bot_obv_divergence" />
+      <option name="ENV_FILES" value="" />
+      <option name="INTERPRETER_OPTIONS" value="" />
+      <option name="PARENT_ENVS" value="true" />
+      <envs>
+        <env name="PYTHONUNBUFFERED" value="1" />
+      </envs>
+      <option name="SDK_HOME" value="" />
+      <option name="WORKING_DIRECTORY" value="$PROJECT_DIR$" />
+      <option name="IS_MODULE_SDK" value="true" />
+      <option name="ADD_CONTENT_ROOTS" value="true" />
+      <option name="ADD_SOURCE_ROOTS" value="true" />
+      <option name="SCRIPT_NAME" value="$PROJECT_DIR$/start.py" />
+      <option name="PARAMETERS" value="" />
+      <option name="SHOW_COMMAND_LINE" value="false" />
+      <option name="EMULATE_TERMINAL" value="false" />
+      <option name="MODULE_MODE" value="false" />
+      <option name="REDIRECT_INPUT" value="false" />
+      <option name="INPUT_FILE" value="" />
+      <method v="2" />
+    </configuration>
+    <recent_temporary>
+      <list>
+        <item itemvalue="Python.start" />
+        <item itemvalue="Python.start" />
+        <item itemvalue="Python.start" />
+      </list>
+    </recent_temporary>
+  </component>
+  <component name="SharedIndexes">
+    <attachedChunks>
+      <set>
+        <option value="bundled-python-sdk-0509580d9d50-746f403e7f0c-com.jetbrains.pycharm.community.sharedIndexes.bundled-PC-241.14494.241" />
+      </set>
+    </attachedChunks>
+  </component>
+  <component name="SpellCheckerSettings" RuntimeDictionaries="0" Folders="0" CustomDictionaries="0" DefaultDictionary="application-level" UseSingleDictionary="true" transferred="true" />
+  <component name="TaskManager">
+    <task active="true" id="Default" summary="Default task">
+      <changelist id="70af1cd2-67dc-4623-85ec-ad4cb930180c" name="Changes" comment="" />
+      <created>1776486283986</created>
+      <option name="number" value="Default" />
+      <option name="presentableId" value="Default" />
+      <updated>1776486283986</updated>
+    </task>
+    <task id="LOCAL-00001" summary="торговый бот на индикаторе obv_divergence">
+      <option name="closed" value="true" />
+      <created>1776606472395</created>
+      <option name="number" value="00001" />
+      <option name="presentableId" value="LOCAL-00001" />
+      <option name="project" value="LOCAL" />
+      <updated>1776606472395</updated>
+    </task>
+    <task id="LOCAL-00002" summary="mod config">
+      <option name="closed" value="true" />
+      <created>1776607925807</created>
+      <option name="number" value="00002" />
+      <option name="presentableId" value="LOCAL-00002" />
+      <option name="project" value="LOCAL" />
+      <updated>1776607925808</updated>
+    </task>
+    <task id="LOCAL-00003" summary="create branch features :mod bot: find_obv_divergence.py, trading.py, start,py">
+      <option name="closed" value="true" />
+      <created>1776617831818</created>
+      <option name="number" value="00003" />
+      <option name="presentableId" value="LOCAL-00003" />
+      <option name="project" value="LOCAL" />
+      <updated>1776617831818</updated>
+    </task>
+    <task id="LOCAL-00004" summary="mod loger in trading.py">
+      <option name="closed" value="true" />
+      <created>1776618375003</created>
+      <option name="number" value="00004" />
+      <option name="presentableId" value="LOCAL-00004" />
+      <option name="project" value="LOCAL" />
+      <updated>1776618375003</updated>
+    </task>
+    <option name="localTasksCounter" value="5" />
+    <servers />
+  </component>
+  <component name="Vcs.Log.Tabs.Properties">
+    <option name="TAB_STATES">
+      <map>
+        <entry key="MAIN">
+          <value>
+            <State>
+              <option name="FILTERS">
+                <map>
+                  <entry key="branch">
+                    <value>
+                      <list>
+                        <option value="features" />
+                      </list>
+                    </value>
+                  </entry>
+                </map>
+              </option>
+            </State>
+          </value>
+        </entry>
+      </map>
+    </option>
+  </component>
+  <component name="VcsManagerConfiguration">
+    <MESSAGE value="торговый бот на индикаторе obv_divergence" />
+    <MESSAGE value="mod config" />
+    <MESSAGE value="create branch features :mod bot: find_obv_divergence.py, trading.py, start,py" />
+    <MESSAGE value="mod loger in trading.py" />
+    <option name="LAST_COMMIT_MESSAGE" value="mod loger in trading.py" />
+  </component>
+</project>
\ No newline at end of file
Index: .idea/inspectionProfiles/profiles_settings.xml
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/.idea/inspectionProfiles/profiles_settings.xml b/.idea/inspectionProfiles/profiles_settings.xml
new file mode 100644
--- /dev/null	
+++ b/.idea/inspectionProfiles/profiles_settings.xml	
@@ -0,0 +1,6 @@
+<component name="InspectionProjectProfileManager">
+  <settings>
+    <option name="USE_PROJECT_PROFILE" value="false" />
+    <version value="1.0" />
+  </settings>
+</component>
\ No newline at end of file
